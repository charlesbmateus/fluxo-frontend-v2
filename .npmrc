# Suppress npm audit warnings for vulnerabilities without non-breaking fixes
# nanotar path traversal vulnerability (GHSA-92fh-27vv-894w)
# This is a moderate severity vulnerability in nanotar <=0.2.0
# There is currently no patched version available, and the only "fix" 
# would be to downgrade Nuxt from 4.x to 3.x (breaking change)
# Monitor for nanotar updates and remove this suppression when fixed
audit-level=high
